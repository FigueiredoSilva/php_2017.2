<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8">
    <title> Exercício 04 Lista 00</title>
</head>
<body> 
    <fieldset> 
        <legend>Formulário</legend>
        <form action="#" method="post"> 
            <label for="quantidade_">Quantidade de Carros Vendidos</label>
            <input type="number" name="quantidade" id="quantidade_">
            
            <button type="submit">Enviar</button>
        </form>
    </fieldset>
    <!-- Codigo PHP -->
    <?php
            if(isset($_POST['quantidade']) ) {
                
                $quantidade = $_POST['quantidade'];
                        
                 $total = ($quantidade)
                
                # Quantidade de carros vendidos * Comissão + Salario
                
                    $quantidade = $_distancia / ($_litros * $_valor);
                    echo 'O salário atual é: $_quantidade';
            }
         ?>
</body>
</html>